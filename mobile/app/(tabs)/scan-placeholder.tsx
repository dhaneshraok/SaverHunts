// This file exists only to satisfy Expo Router's file-based routing.
// The "Scan" tab is a FAB that triggers a modal — it never navigates here.
import { View } from 'react-native';
export default function ScanPlaceholder() {
  return <View />;
}
